import {Text, View, SafeAreaView} from 'react-native';
import React from 'react';
import {appStyle} from '../Style/Style';

const Status = () => {
  return (
    <View>
      <Text style={[appStyle.pageTitle]}>Status page</Text>
    </View>
  );
};

export default Status;
