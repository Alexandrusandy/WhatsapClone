import React from 'react';
import {Text, View} from 'react-native';
import {appStyle} from '../Style/Style';

const Camera = () => {
  return (
    <View>
      <Text style={[appStyle.pageTitle]}>Camera Page</Text>
    </View>
  );
};

export default Camera;
